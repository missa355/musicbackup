/// <reference types="react" />
interface Props {
    total: number | void;
    value: number;
    color: string;
    background: string;
    completeBackground: string;
    seek: (value: number) => void;
    size: number;
}
export declare const ProgressBar: ({ background, completeBackground, color, seek, total, value, size }: Props) => JSX.Element;
export {};
